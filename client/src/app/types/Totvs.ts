export type Dataservers = {
  id: string;
  code: string;
  name: string;
  name_alternative: string;
};

export type Process = {
  id: string;
  code: string;
  name: string;
  name_alternative: string;
};
